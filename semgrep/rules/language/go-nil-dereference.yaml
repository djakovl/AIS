rules:
  - id: go-nil-pointer-sessionid
    languages: [go]
    severity: WARNING
    message: >
      Обращение к срезу строки sessionID[:8] без проверки длины —
      паника при len < 8.
    pattern: $VAR[:8]
    metadata:
      cwe: "CWE-476"